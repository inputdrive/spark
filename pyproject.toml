[project]
name = "agentic-audit-tools"
version = "0.1.0"
description = "Read-only local audit inventory for agentic coding tools"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
agentic-audit-tools = "agentic_audit.cli:main"

[tool.setuptools]
packages = ["agentic_audit"]

[tool.pytest.ini_options]
pythonpath = ["."]
